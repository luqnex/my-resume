type themeProps = {
  primary: string;
  headingColor: string;
  bgShade: string;
  dribble: string;
  body: string;
  black: string;
  white: string;
};

export const theme: themeProps = {
  primary: "#5E3BEE",
  headingColor: "#282938",
  bgShade: "#F5FCFF",
  dribble: "#E62872",
  body: "#1C1E53",
  black: "#000",
  white: "#FFF",
};
